# Релятивистское движение заряженной частицы в скрещённых полях

Численное моделирование траектории и фактора Лоренца `γ(t)` для частицы в постоянных полях `E ⟂ B`. Интегратор — RK4.

## Установка
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
